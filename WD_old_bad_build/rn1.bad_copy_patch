#!/usr/bin/env bash
set -euo pipefail

inlist_name="${1:-inlist}"

echo "run ${inlist_name}"

if [ ! -f "${inlist_name}" ]; then
    echo "ERROR: missing inlist: ${inlist_name}"
    exit 1
fi

cp "${inlist_name}" inlist
./star
